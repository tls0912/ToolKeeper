# Rendering

Markdown Preview 相關邏輯集中在這裡。

預計：

- MarkdownRenderer
- Preview HTML / CSS
- Safe HTML handling
- Heading / anchor mapping
- Edit ↔ Preview position mapping

Markdig 與 WebView2 的細節盡量留在此區域，不往其他模組擴散。
